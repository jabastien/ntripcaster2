## Process this with automake to create Makefile.in

AUTOMAKE_OPTIONS = foreign

all: 

etcdir = $(NTRIPCASTER_ETCDIR_INST)
etc_DATA = groups.aut.dist ntripcaster.conf.dist clientmounts.aut.dist sourcemounts.aut.dist users.aut.dist sourcetable.dat.dist

EXTRA_DIST = $(etc_DATA)
